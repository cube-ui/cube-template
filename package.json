{
  "name": "vue-cli-template-cube-ui",
  "version": "1.0.0",
  "license": "MIT",
  "description": "cube-ui template base on webpack for Vue-cli",
  "scripts": {
    "demo": "rm -rf demo && vue init ./ demo"
  },
  "devDependencies": {
    "vue-cli": "^2.8.1"
  }
}
